{
  "name": "noren-english",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --force",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:tz": "node scripts/run-tz-tests.mjs",
    "data:fetch": "node --experimental-strip-types scripts/fetch-tatoeba.ts",
    "data:build": "node --experimental-strip-types scripts/build-sentences.ts",
    "data:check": "vitest run tests/data.check.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260818.1",
    "@types/node": "^26.2.0",
    "happy-dom": "^20.11.2",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.10"
  }
}
