# のれん (Noren) — 実装計画書

1日5分だけ開く英語学習アプリ。
このドキュメントはClaude Codeへの指示書として書かれている。

---

## 0. 何を作るか

**開いている時間だけ、問題が出る。**

「のれん」は名前としてだけ残す。暖簾の見立て（店先・横木・布が下りる）は使わない。
世界観で説明するのをやめ、**語学アプリとして素直に使える形**にする。

1日1回、自分で決めた時間帯の中のランダムな時刻に開店する。そこから5分だけ英文1つに取り組める。逃せばその日の問題は消える。

倒す敵は **「いつでもできる」が「いつまでもやらない」を作っている** こと。だから時間の側から制約をかける。

### 絶対に守る制約

1. **運用費0円**（利用者が何人増えても開発者の負担が増えない）
2. **アクセスするだけで使える**（アカウント登録なし）
3. **有料AI APIを呼ばない**（ブラウザ内蔵APIのみ可）

---

## 1. データ保存

### サーバーに学習データを送らない

学習記録・時間帯設定・チケット残数、すべて `localStorage`。通信はアプリ本体と例文JSONの取得だけ。

```js
// localStorage キー設計
noren:salt        // string  初回に crypto.randomUUID()
noren:settings    // { notifyStart, notifyEnd, mode, installedPrompted }
noren:records     // [{ date, opened, solved, accuracy, sentenceId, mode }]
noren:tickets     // { count, refilledWeek }
noren:milestones  // { seen7, seen14, seen21, seen30 }
```

### 唯一の例外：通知の購読情報

Web Pushはプッシュサービス経由なので、購読情報だけはサーバーに要る。保存するのはこれだけで、学習内容は一切送らない。

```sql
CREATE TABLE push_subscriptions (
  endpoint     TEXT PRIMARY KEY,
  p256dh       TEXT NOT NULL,
  auth         TEXT NOT NULL,
  notify_start INTEGER NOT NULL,   -- 分 (0-1439)
  notify_end   INTEGER NOT NULL,
  tz_offset    INTEGER NOT NULL,
  salt         TEXT NOT NULL,      -- 開店時刻の算出に必要
  created_at   INTEGER NOT NULL
);
```

`salt` をサーバーにも渡すのは、同じ計算をしないと「何時に送ればいいか」が分からないため。個人を特定できる情報は含まない。

**通知を許可しなければサーバーへの登録は一切発生しない。** 通知オフでも全機能が使える設計にすること（自分で見に来る運用）。

### ⚠ 最重要：iOSでlocalStorageが7日で消える

Safariは**7日間アクセスがないサイトのlocalStorageを削除する**（ITP）。30日の継続を狙うアプリにとって致命的。

対策は3つ全部やる。

1. **ホーム画面に追加させる。** インストール済みPWAはこの削除対象外。iOSではWeb Pushもホーム画面追加が必須なので、どのみち通す導線。初回に専用の案内画面を出す
2. **書き出し / 読み込み。** 全記録をJSONでダウンロード、読み込みで復元。1画面で済む。バックアップであると同時に「自分のデータは自分のもの」という思想の表明になる（提出物の解説で書ける）
3. **7日目のマイルストーンをこの制約に合わせる。** 7日目までにホーム画面へ追加しなかった人は、そもそも消える

**Phase 1の段階でホーム画面追加の導線だけ先に作ること。** Phase 4で気づくと手戻りが大きい。

---

## 2. 状態機械

```
      ┌─────────────┐
      │   初回設定   │ 時間帯を選ぶ → ホーム画面追加を案内
      └──────┬──────┘
             ↓
   ┌────► 閉店中 ─────┐
   │        │          │
   │   開店時刻に到達   │ チケットを使う
   │        ↓          ↓
   │     開店中(5分)  延長(3分)
   │        │          │
   │        └────┬─────┘
   │             ↓
   │          出題・解答
   │             ↓
   │           結果表示
   │             ↓
   └──────── 記録して閉店
```

日付境界は **JSTの午前4時**。深夜に解く人が「日をまたいだせいで2日消費した」とならないように（利用実態に合わせた設計として解説に書ける）。

---

## 3. 開店時刻のアルゴリズム

サーバーを使わず、かつリロードしても変わらない値が必要。日付とsaltから決定論的に導く。

```js
// salt: 初回に生成して localStorage に保存
// dateKey: "2026-08-18" (JST午前4時基準)
// start, end: 分単位 (21:00 → 1260, 23:00 → 1380)

async function openMinute(salt, dateKey, start, end) {
  const buf = new TextEncoder().encode(salt + dateKey);
  const hash = await crypto.subtle.digest("SHA-256", buf);
  const n = new DataView(hash).getUint32(0);
  return start + (n % (end - start));
}
```

**同じ関数をWorker側にも置く**（通知送信のため）。片方だけ直すとズレるので、1ファイルに切り出してフロントとWorkerの両方から読む。

- 時間帯は最短60分・最長360分。狭すぎると固定時刻と変わらず、広すぎると張り込みが辛い
- 時間帯の変更は**翌日から反映**。当日変更を許すと総当たりで開店時刻を探せてしまう

---

## 4. 出題

### モードA：音読（既定）

英文をTTSで再生 → マイクで音読 → 認識結果と原文を単語単位で比較。

```js
const rec = new (window.SpeechRecognition || window.webkitSpeechRecognition)();
rec.lang = "en-US";
rec.interimResults = false;
```

判定は完全一致ではなく単語アラインメント。

1. 両方を小文字化、句読点除去、トークン化
2. LCS（最長共通部分列）で対応付け
3. 一致=緑 / 欠落=灰 / 誤り=赤 を原文の上に重ねる
4. 一致率 = 一致トークン数 ÷ 原文トークン数

**発音の良し悪しは判定していない**（認識器が拾えたかを見ているだけ）。これは画面上にも正直に書くこと。誇大表示は信頼を壊す。

### モードB：並べ替え（フォールバック兼、声が出せない時）

同じ英文の単語をシャッフルして並べ替え。SpeechRecognition非対応なら自動でこちら。

対応状況は実機で必ず確認する。Firefoxは未対応、iOS Safariはバージョンで挙動が違う。**モードBが常に動けば、どの端末でもアプリは成立する**作りにしておく。

---

## 5. 例文データ

**Tatoeba**（CC-BY 2.0 FR）から事前抽出してJSONに固める。実行時に外部APIを叩かない。

- 英文＋日本語訳のペアが揃っているものだけ
- 5〜14語（音読1回が20秒以内）
- 語彙頻度リストで3段階の難易度に分類
- 1000文で3年分。ファイルは数百KB
- **ライセンス表示を設定画面に必ず置く**

日付とグローバル定数（**saltを含まない**）から選ぶ = 全世界で同じ1文。Wordle型の要なので、ここにユーザー固有値を混ぜないこと。

---

## 6. マイルストーン

連続日数は**どこにも表示しない**。代わりに未見の報酬を離脱点の直前に置く。

| 参加日数 | 解放されるもの |
|---|---|
| 1〜6 | なし。解いて閉じるだけ |
| **7** | 今週の7文が一覧になる |
| **14** | 7日前の文を抜き打ちで再出題（間隔反復） |
| **21** | 一致率の推移グラフが初めて出る |
| **30** | 30日カードを生成（画像として保存・共有） |

30日カードは30マスの石畳。参加した日は石が置かれ、**欠けた日は空白のまま描く**。全部埋まっている必要はない、という思想をビジュアルで示す。SVGで生成し `canvas` 経由でPNG化。

---

## 7. チケット

- 週2枚。JST月曜4時に補充。繰り越しなし
- 閉店後に使うと3分だけ開く（通常5分）
- 「使う」は閉店中の画面下部に小さく置く。目立たせない
- 残数は常時表示。減るのが見えることに意味がある

---

## 8. 通知

**完全サーバーレスでの定時通知は実現できない。** Service Workerは常駐せず、端末がスリープすればタイマーは止まる。正直にこう設計する。

- **Phase 1〜2：通知なしで作る。** 「今日は21:47に開きます」と表示して、自分で見に来てもらう。これで全機能が動く
- **Phase 4：Cloudflare Workers + Cron Trigger。** 毎分起動し、その分に開店する購読者を抽出してWeb Push。VAPIDキーはWorker Secretへ

通知本文に問題は含めない。「開きました（残り5分）」だけ。中身を見るには開く必要がある。

---

## 9. 画面

**待っているあいだ**
```
┌──────────────────────┐
│ 設定                 │
│ ┌──────────────────┐ │
│ │   次に開くのは    │ │
│ │      21:47       │ │  ← カード
│ └──────────────────┘ │
│ ┌──────────────────┐ │
│ │ 今週              │ │
│ │ 月 火 水 木 金 土 日│ │
│ │ ○ ● ● ・ ・ ・ ・ │ │  ← やった日だけ置かれる
│ └──────────────────┘ │
│                      │
│         [チケット 2] │
└──────────────────────┘
```
ボタンはほぼ無い。設定とチケットだけ。

**開いているあいだ**
```
┌──────────────────────┐
│ ████████████░░░░░░░░ │  ← 残り時間のバー
│                 4:12 │
│  彼女は1時間待っている │  ← 並べ替えは和訳が手がかり
│                      │
│  [ She ][ has ]      │
│  ─────────────────   │
│  [ been ][ waiting ] │  ← 語のタイル
│                      │
│ ┌──────────────────┐ │
│ │      できた      │ │  ← 主操作は画面の一番下
│ └──────────────────┘ │
│      ひとつ戻す       │
└──────────────────────┘
```

**結果**
```
┌──────────────────────┐
│  She has been        │
│  ▔▔▔ ▔▔▔ ▔▔▔▔        │  緑 = 合っていた語
│  waiting for an      │
│  ▔▔▔▔▔▔▔ ▔▔▔         │  朱 = 違っていた語
│  hour.               │
│  彼女は1時間待っている │
│ ┌──────────────────┐ │
│ │ ほとんど合っています│ │  ← 帯。色は判定にだけ対応
│ │       89%        │ │
│ └──────────────────┘ │
└──────────────────────┘
```
3秒後に自動で待機画面へ戻る。「もう一度」は無い。

---

## 10. デザイン

**白地の語学アプリ。** 迷わず押せて、読みやすいこと。見立てや世界観で飾らない。

- **地は白**。ダークテーマは作らない。面はカード（薄灰 `#F6F7F9` / 角丸18px）で区切る
- **配色**：白 `#FFFFFF` / 薄灰 `#F6F7F9` / 溝 `#ECEEF2` / 文字 `#1F2430`・`#5B6273`・`#6E7686`
  意味を持つ色は3つだけ。**藍 `#2F5BD8`（押せるもの・進んでいるもの）/ 青緑 `#17794F`（合っていた語）/ 朱 `#CF4436`（違っていた語）**
  色は意味にだけ対応させる。装飾に機能色を使わない。**色数を増やさない**
- **書体**：システムサンセリフのみ。明朝は使わない。見出しと操作は太字（700）
- **押せるものには厚み**：下辺だけ太い枠（4px）が厚みになり、押すと沈む。
  影の濃さや彩度ではなく**形**で「押せる」を示す。白地でこれをやると迷いが消える
- **署名要素**：**残り時間のバー。** 画面上端で5分かけて減る。数字は補助。
  結果画面に切り替わってもバーは残り、時間切れか解き終えたかが動きで分かる
- **動き**：開始と終了だけ。他は動かさない。`prefers-reduced-motion` を尊重
- **タップ対象は最低52px**。指で押す前提で、隣と間を空ける

避けること：既存の語学アプリ（Duolingo など）の**テーマカラーやマスコットの模倣**。
キャラクター・効果音・紙吹雪・連続日数の煽り。装飾のためのイラスト。
足したくなったら減らす。

---

## 11. 技術スタック

| | |
|---|---|
| フロント | Vite + TypeScript（フレームワークなし） |
| PWA | vite-plugin-pwa |
| ホスティング | Cloudflare Pages |
| バックエンド | Cloudflare Workers（通知のみ） |
| DB | localStorage（購読情報だけD1） |
| 状態管理 | 素のTS |

フレームワークを入れないのは、画面が3つしかなく、**起動速度が体験の中心**だから。「開いた瞬間に問題が出る」ことが5分の価値を決める。

---

## 12. 実装フェーズ

各フェーズの終わりに必ず動く状態にすること。1フェーズ = 1ブランチ = 1 PR。

### Phase 1 — 核
- Vite + TS + PWA の雛形
- 例文JSONの生成スクリプト（Tatoebaから抽出・整形）
- 開店時刻アルゴリズム + JST午前4時の日付境界
- 3画面（閉店中 / 開店中 / 結果）
- **並べ替えモードだけ**実装（音声はまだ）
- localStorageの読み書き
- **ホーム画面追加の導線**（後回しにしない）

この時点で、通知なしでも1日1問が回る。

### Phase 2 — 音声と報酬
- Web Speech API（TTS + STT）、非対応時のフォールバック
- LCSによる単語アラインメントと色分け
- マイルストーン 7/14/21/30
- 30日カードのSVG生成 → PNG書き出し
- チケット
- 書き出し / 読み込み

### Phase 3 — 通知
- VAPIDキー生成、Worker Secretに登録
- 購読の登録・解除
- Cron Trigger（毎分）でpush
- 開店時刻アルゴリズムをWorker側と共有

### Phase 4 — 検証準備
- 利用ログ（開いた/解いた/逃した、日付のみ）をローカルに残す
- 書き出したJSONから集計するスクリプト

**Phase 4を軽視しないこと。** 課題の「試用・改善」「振り返り」はここのデータで書く。

---

## 13. 検証計画

### 自分で試す（最低7日、できれば14日）

毎日1行、**アプリの外に手で**記録する。

```
日付 / 開いたか / 逃した理由 / 5分は長かったか短かったか / 面倒だと感じた瞬間
```

**「面倒だと感じた瞬間」が一番価値のあるデータ。** うまくいった話より、こちらを詳しく書く。

### 他人に試してもらう

3〜5人に7日間。**通知を切った人が何人出たかを必ず聞く。** ここが設計の急所なので、切られたなら理由まで聞き出す。

### 記録しておくべき問い

- ランダム性は緊張感になったか、単なる面倒になったか
- チケットは救いになったか、それとも言い訳になったか
- 5分という長さは適切だったか
- 7日目の報酬は、7日目まで持たせる力があったか

---

## 14. Claude Codeへの渡し方

1. このファイルをリポジトリ直下に `SPEC.md` として置く
2. `CLAUDE.md` に短く書く：「SPEC.mdが唯一の仕様。フェーズ単位で進め、各フェーズ終了時に必ず動く状態にする。仕様に無いライブラリを足す前に確認すること」
3. Phase 1が終わったら**必ず実機で触る**。特にiPhone。ここで設計の嘘が全部出る

### 先に潰れる可能性が高い順

1. **iOS SafariのSpeechRecognition** — 動かない前提で、モードBを主役にできる作りに
2. **iOSのlocalStorage 7日削除** — ホーム画面追加の導線が弱いと30日検証が全滅する
3. **Web Push on iOS** — ホーム画面追加が必須。Phase 3で判明すると手戻りが大きい
