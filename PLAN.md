# PLAN.md — Noren 改善計画

Phase 1（出題ループ）が動く状態からの続き。
`SPEC.md` が仕様、この `PLAN.md` が作業順。仕様と食い違ったら `SPEC.md` が優先。

## 進め方

- **1タスク = 1ブランチ = 1 PR。** タスクIDをブランチ名に使う（例 `p0-1-timer`）
- 各PRの終わりに `npx tsc -b` と `npx vite build` が両方通ること
- 完了条件（DoD）を満たさないうちに次へ進まない
- 仕様に無いライブラリを足す前に確認する
- UIは `src/styles/tokens.css` のトークンだけを使う。独自の色・影・角丸を足さない

## 現状

動くもの：3分の出題ループ、3形式（空所補充・並べ替え・和訳選択）、難易度の上下、
ポイント、バッジ、パズル40ピース、下部タブ4つ、localStorage、ダークモード。

動かないもの：時間の制約、通知、音読、PWA。例文は60文しかない。

---

# P0 — 先に直す（既存コードの不具合）

Phase 2 に進む前に片付ける。土台が歪んだまま機能を積むと後で全部やり直しになる。

## P0-1 タイマーを実時刻ベースにする

**対象**：`src/screens/Session.tsx`

現在 `setInterval` で `elapsed += 0.1` と累算している。これは2つ壊れている。

1. **ドリフトする。** `setInterval` は正確に100msでは呼ばれないので、3分のセッションが実際には3分10秒などになる
2. **バックグラウンドで止まる。** 他アプリに切り替えるとブラウザがタイマーを間引くため、戻ってきたときに時間が余っている。**3分の制約が意味を失う**

やること：

- セッション開始時刻を `Date.now()` で記録し、経過は毎回 `Date.now() - startedAt` で算出する
- 問題ごとの制限時間も同様に、その問題の開始時刻からの差分で判定する
- `setInterval` は「再描画のきっかけ」としてのみ使う（値の源にしない）
- `visibilitychange` で画面に戻ったとき、即座に再計算して時間切れなら終了処理へ

**DoD**：セッション中に別アプリへ切り替えて30秒後に戻ると、残り時間が30秒減っている。3分のセッションが実測で3分±1秒に収まる。

## P0-2 StrictMode の二重実行で問題が2回生成される

**対象**：`src/screens/Session.tsx`

マウント時の `useEffect` で `nextQuestion()` を呼んでいるため、開発時の StrictMode で2回走り、
`recentIds` に同じ文が二重に積まれる。

やること：初回の問題は `useState` の初期化関数で作るか、`useRef` で初回実行をガードする。

**DoD**：開発モードで `recentIds` に重複が入らない。

## P0-3 開始レベルが仕様とずれている

**対象**：`src/lib/store.ts`, `src/screens/Session.tsx`

仕様は「開始レベル = **前回の終了レベル** − 1」。
現在は `maxLevel`（セッション中の最高到達レベル）を保存しているため、
一度でも高いレベルに触れると次回が不当に高く始まる。

やること：`SessionRecord` に `endLevel` を追加し、`progress.lastLevel` はそちらを見る。
`maxLevel` は結果表示用として残す。

**DoD**：Lv.4 まで上がって Lv.2 で終えたセッションの次回開始が Lv.1 になる。

## P0-4 例文が足りず、すぐ重複する

**対象**：`src/data/sentences.ts` + 新規スクリプト

各レベル12文しかない。`recentIds` は直近8件しか避けないので、3分回すだけで同じ文が再登場する。
**これは体験上いちばん大きな欠陥。**

やること：`scripts/build-sentences.ts` を作り、Tatoeba（CC-BY 2.0 FR）から抽出する。

- 英日ペアが揃っているもの
- 5〜14語
- 語彙頻度リストでレベル1〜5に分類（分類の閾値は実文を見て調整）
- 空所補充の誤答を機械生成する。**活用形・前置詞・語順**の3系統から作る
- 出力は既存の `Sentence[]` 型に合わせる
- **各レベル200文以上**

`validateSentences()` が blankIndex と answer の不一致を検出するので、生成後に必ず走らせる。

**注意**：誤答の質がこのアプリの核。機械生成した選択肢は
「明らかにおかしくて読まずに消去法で解ける」ことが多い。
**生成後に100問ほど目視すること。** ここを飛ばすと、正答率が高く出ているのに
何も身についていない状態になる。

**DoD**：各レベル200文以上。`validateSentences()` が0件。3分回して同じ文が2回出ない。

## P0-5 誤答判定のばらつきを潰す

**対象**：`src/lib/questions.ts`

和訳選択の誤答を「他の文の日本語訳」からランダムに取っているため、
明らかに文脈の違う訳が並び、英文を読まなくても消去法で解けることがある。

やること：同レベル、かつ語数が近い文から誤答を選ぶ。

**DoD**：和訳選択で、英文を隠しても正解が推測できる問題が体感で減っている。

---

# P1 — Phase 2：時間の制約

ここまでで「アプリ」になる。P0が終わってから着手する。

## P1-1 3枠の設定画面

**対象**：`src/screens/Tabs.tsx`（Settings）、`src/lib/store.ts`

- 朝・昼・夜の3枠。それぞれ **開始時刻・終了時刻・有効/無効**
- 既定値：朝 7:00–8:00 / 昼 12:30–13:30 / 夜 21:00–23:00
- 時間帯の幅は **最短60分・最長360分**。範囲外はUIで選べないようにする
- **各枠は個別にオフにできる。** 昼枠が現実的でない人がいる
- 変更は**翌日から反映**。当日変更を許すと総当たりで開始時刻を探せてしまう
- `noren:settings` に保存

**DoD**：3枠を設定・保存でき、リロードしても保持される。当日分の開始時刻が変わらない。

## P1-2 開始時刻アルゴリズム

**対象**：新規 `src/lib/schedule.ts`

```ts
// salt: 初回に crypto.randomUUID() で生成し noren:salt に保存
// dateKey: JST午前4時基準
// slot: 0 | 1 | 2
async function openMinute(salt, dateKey, slot, start, end): Promise<number>
```

SHA-256 の先頭4バイトを使い、`start + (n % (end - start))` を返す決定論的な実装。
**この関数は後で Worker 側からも呼ぶ**ので、React に依存させない単独モジュールにすること。

**DoD**：同じ入力で常に同じ値。リロードしても当日の開始時刻が変わらない。

## P1-3 ホーム画面を待機／開始可能の2状態にする

**対象**：`src/screens/Tabs.tsx`（Home）

- **待機中**：「次は 12:47ごろ / 昼の回」＋ パズルの縮小表示 ＋「あと12ピース」
- **開始可能**：「いま開いています / 残り 4分12秒」＋ 大きい「始める」ボタン
- 開始可能な時間は**開始時刻から5分間**。3分のセッションはボタンを押してから始まる
- この2つの時間を混同させないよう、ラベルで明示する
- **今日やった回数は「今日 2回」と表示する。分母（3回中）を出さない**

**DoD**：時刻を進めると待機中→開始可能→待機中と遷移する。分母がどこにも出ていない。

## P1-4 PWA化とホーム画面追加の導線

**対象**：`vite.config.ts`、新規 `src/components/InstallPrompt.tsx`

`vite-plugin-pwa` を入れ、manifest とアイコンを用意する。

**iOS Safari は7日間アクセスがないサイトの localStorage を削除する（ITP）。**
インストール済みPWAは対象外なので、これは体裁ではなく**データ保全の中核**。

- iOS Safari かつ未インストールを検出したら、案内を出す
- 「共有 → ホーム画面に追加」の手順を図で示す
- 初回起動時に1回、その後は3日後にもう1回。**それ以上は出さない**

**DoD**：iPhone 実機でホーム画面に追加でき、スタンドアロンで起動する。

---

# P2 — Phase 3：報酬の仕上げ

## P2-1 パズルの絵を差し替える

**対象**：`src/lib/puzzleArt.ts`

現在は手続き生成の仮画像。パブリックドメインの美術作品に差し替える。

- Met Museum Open Access / Rijksmuseum / Art Institute of Chicago（すべて CC0）
- 10〜20枚、1600px程度にリサイズして同梱。**実行時に外部APIを叩かない**
- 完成時に**作品名・作者・年代を英語で**表示する（報酬が学習内容と繋がる）
- 出典とライセンスを設定画面に記載

**構図がはっきりした絵を選ぶこと。** 抽象画や単色に近い絵は、
ピースが開いても何も起きていないように見える。

**DoD**：10枚以上同梱され、完成時に英語で作品情報が出る。

## P2-2 1枚目完成時のバックアップ案内

**対象**：`src/screens/Tabs.tsx`（Puzzle）

いまバックアップは設定画面の奥にあり、**誰も押さない**。

1枚目のパズルが完成した瞬間に案内を出す。
ここが「守る価値のあるものができた」最初の瞬間なので、この配置は意図的。

- JSON の自動ダウンロード。ボタン1つ
- 断れる。しつこく出さない

**DoD**：1枚完成した直後に案内が出て、ファイルが落ちる。

## P2-3 読み込み（復元）

**対象**：`src/screens/Tabs.tsx`（Settings）

書き出しはあるが読み込みが無い。バックアップとして片手落ち。

- ファイル選択で JSON を読み、`sessions` と `progress` を復元
- 復元前に確認ダイアログ。**現在の記録が上書きされることを明示する**

**DoD**：書き出した JSON で別ブラウザに復元できる。

---

# P3 — Phase 4：通知と調整

## P3-1 Web Push

**対象**：新規 `worker/` ディレクトリ

- Cloudflare Workers + D1 + Cron Trigger（毎分）
- VAPID キーは Worker Secret に置く
- `push_subscriptions` テーブルは `SPEC.md` の定義どおり
- `openMinute()` を **P1-2 のモジュールから import して共有する**。二重実装しない
- 通知本文は「開きました（残り5分）」だけ。**問題は含めない**

**通知は3枠それぞれ個別にオフにできること。** 1日3通は多いと感じる人が必ずいる。
ここを強制すると通知ごと切られ、アプリが死ぬ。

**DoD**：iPhone（ホーム画面追加済み）と Android で通知が届く。枠ごとにオフにできる。

## P3-2 音読モードを10%枠で追加

**対象**：`src/lib/questions.ts`、新規 `src/components/SpeechQuestion.tsx`

- Web Speech API（TTS + STT）
- LCS で単語アラインメント、一致=緑／欠落=灰／誤り=赤
- **非対応環境ではこの10%を並べ替えに振り替える。** 音読が動かなくてもアプリは完全に成立する
- 「発音の良し悪しは判定していない（認識器が拾えたかを見ている）」と画面に明記する

Firefox は未対応、iOS Safari はバージョンで挙動が違う。**動かない前提で組む。**

**DoD**：Chrome で動き、Firefox でエラーにならず並べ替えが出る。

## P3-3 バッジしきい値の実測調整

**対象**：`src/lib/session.ts` の `BONUS_THRESHOLD`

現在150ptは根拠のない仮の値。**目安は「2回に1回取れる」くらい。**

自分と検証協力者のセッション記録から、ポイント分布を集計して決める。
高すぎると誰も取れず、低すぎると意味がなくなる。

**DoD**：実データ20セッション以上に基づいて数値が決まり、根拠がコメントに書いてある。

## P3-4 利用ログ

**対象**：`src/lib/store.ts`

課題の「試用・改善」「振り返り」に使うデータ。**ここが無いと提出物が書けない。**

記録するのは日付と種別だけ（開いた／やった／逃した）。内容は残さない。
書き出した JSON から集計するスクリプトも用意する。

**DoD**：30日分のログを集計して、実施率・平均問題数・正答率・到達レベル分布が出る。

---

# 実装で崩れやすい設計判断

機能追加のたびに壊れやすいので、PRレビュー時に毎回確認すること。

1. **「今日 N回」に分母を出さない。** 「2/3」と表示した瞬間に、1回逃した人が失敗者になる。1回でも達成
2. **連続日数を表示しない。** 途切れた瞬間の落胆が離脱を生む。累計で見せる
3. **結果画面に間違えた問題の一覧を出さない。** 達成感が反省に上書きされる。記録タブで後から見せる
4. **通知本文に問題を含めない。** 開かせるための通知が、開かなくても済む通知になる
5. **色は意味にのみ対応させる。** 装飾に機能色を使わない

---

# 順番の根拠

P0 を先にやるのは、**タイマーと例文が壊れたまま時間の制約を載せても検証にならない**から。
3分が実際は3分10秒で、同じ問題が繰り返し出る状態で「1日3回」を試しても、
得られるフィードバックが設計の良し悪しなのか実装バグなのか切り分けられない。

P1 が終わった時点で**自分の7日間の試用を開始できる**。
P2・P3 はその試用と並行して進めてよい。
検証は日数がかかるので、**始められる状態を早く作るのが最優先**。